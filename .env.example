# Path to the SQLite database file (relative to project root)
DB_FILE_NAME=.data/workout.db

# `bun run dev:solo` loads .env.solo instead of .env — keep it free of the
# Google OAuth vars (that's what forces single-user mode) and point
# DB_FILE_NAME at a separate file so solo testing never touches the dev db.
