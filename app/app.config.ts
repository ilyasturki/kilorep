export default defineAppConfig({
    ui: {
        colors: {
            primary: 'emerald',
            // Drives every dark surface; zinc drops slate's blue cast for a clean neutral gray.
            neutral: 'zinc',
        },
    },
})
