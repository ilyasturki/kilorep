# Generated by drizzle-kit. Committed because the server applies them at boot,
# but reformatting them churns the diff on every regenerate.
/drizzle/

# Checkouts of this same repo. Formatting them edits another branch's working
# tree, which is never this run's job.
/.claude/worktrees/

# Generated by `cap add android` and rewritten by `cap sync`. Gradle and Java
# have their own conventions and prettier knows none of them; reformatting the
# XML would churn the diff on every sync for no reader's benefit.
/android/

# Prose is hand-authored — see the docs/ set.
*.md
