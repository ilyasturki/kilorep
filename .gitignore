# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist
# Generated module-setup marker (written by @nuxt/test-utils on prepare)
.nuxtrc

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example
!.env.test

# Nix build symlink
result
result-*

# Claude Code project MCP config (may contain bearer tokens)
.mcp.json

# Android app build outputs (android/)
android/.gradle
android/.kotlin
android/build
android/*/build
android/local.properties

# Exercise illustration source rasters: regenerable (Codex + scripts/illustrations/
# GOAL.md) and large (~240 MB). Only the traced public/illustrations/*.svg ship.
scripts/illustrations/raw/
scripts/illustrations/model-sources/

# design-sync: staged converter scripts, generated bundle, and machine state.
# The durable inputs (.design-sync/config.json, NOTES.md, conventions.md,
# previews/) are committed; everything below is regenerated per run.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
