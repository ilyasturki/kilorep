<?xml version="1.0" encoding="utf-8"?>
<!--
    A placeholder, not an identity. Flat lime-500 — the one accent DESIGN.md
    allows, and the one swatch that is identical in both themes — behind the K
    in `ic_launcher_foreground`. The hex is the sRGB of
    `oklch(76.8% 0.233 130.85)`, Tailwind's lime-500, written out because
    Android resources cannot read app.css.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#7ccf00"
        android:pathData="M0,0h108v108h-108z" />
</vector>
