# Product

Sketch-level, not pixel spec. Clear enough to build from, refined while building.

## Domain model

Entities and their meaning. Free of v1's schema; carry over the domain wins (per-exercise load modes: total / per-hand / unilateral; blank open targets with last-logged hints; honest volume math; raw PRs).

- Exercise:
- Template / planned session:
- Workout (a performed session):
- Set:
- Body weight entry:

## The in-gym loop, set by set

The sacred path. Narrate it: I rack my dumbbells, I pick up the phone, and then...

- Start a session (from template / from scratch):
- During a set: what is on screen, what is one tap away:
- Log a set (target taps: as few as the market bar or fewer):
- Rest:
- Next exercise / reorder / insert mid-workout:
- Finish:

## Screens

Every screen must justify its existence. v1's dashboard died for not having one.

- Screen list:
- Navigation model (no surprises: every tap goes where it obviously goes):

## Offline behavior

- What data lives on the device (enough for the entire in-gym experience with zero bars):
- Sync model (opportunistic, conflict story):
- What happens on first install / on a new device:

## Data-out

- Export formats (CSV/JSON):
- API surface:
- MCP:

## Friction log (from daily use of v1)

Every annoyance hit while still training with v1 goes here, dated, as design input.

-
